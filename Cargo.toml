[package]
name = "ssh-agent-fixer"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "ssh-agent-probe"
path = "src/main.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
libc = "0.2"
